const square = document.querySelectorAll('.square')     // linking to css using .
const mole = document.querySelectorAll('.mole')
const timeLeft = document.querySelectorAll('#time-left')       // linking to html using #

let score = document.querySelector('#score') // using 'let' because it will change

// Start of the game
let result = 0
function randomSquare() {
    square.forEach(className => {
        className.classList.remove
    })
}



















